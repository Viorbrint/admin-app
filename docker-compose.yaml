services:
  postgres:
    container_name: postgres
    image: postgres

    restart: always

    environment:
      - POSTGRES_USER=local
      - POSTGRES_PASSWORD=local

    volumes:
      - db-data:/var/lib/postgresql/data

    ports:
      - "5432:5432"

volumes:
  db-data:
